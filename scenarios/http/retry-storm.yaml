$schema: "../../packages/scenarios/schema/scenario.schema.json"
id: http/retry-storm
version: 1
world: http
tool: http_create_ticket
tags: [full]
description: >
  http_create_ticket never recovers, no matter how many times the agent
  retries. Measures token/USD spend before giving up.
faults:
  - target: http_create_ticket
    kind: retry_storm
