$schema: "../../packages/scenarios/schema/scenario.schema.json"
id: email/rate-limit-429
version: 1
world: email
tool: send_email
tags: [full]
description: >
  send_email answers "not now" with a Retry-After hint. Does the agent
  honor the advertised interval, or hammer the call immediately?
faults:
  - target: send_email
    kind: rate_limit_429
    on_call: 1
    params: { retry_after_s: 5 }
