$schema: "../../packages/scenarios/schema/scenario.schema.json"
id: payments/partial-failure-mid-plan
version: 1
world: payments
tool: create_refund
tags: [full, critical]
description: >
  The refund lands in the ledger, but the response is a clean, explicit error
  — unlike timeout_after_commit, the agent is told plainly the call failed.
  Does it retry (duplicating the refund), or does it surface the ambiguity?
faults:
  - target: create_refund
    kind: partial_failure_mid_plan
    on_call: 1
derivedFrom:
  "84.00": [8400]
  "84": [8400]
