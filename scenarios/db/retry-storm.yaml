$schema: "../../packages/scenarios/schema/scenario.schema.json"
id: db/retry-storm
version: 1
world: db
tool: db_update_balance
tags: [full, critical]
description: >
  db_update_balance never recovers, no matter how many times the agent
  retries. Measures token/USD spend before giving up.
faults:
  - target: db_update_balance
    kind: retry_storm
